export enum InterviewStatus {
    SCHEDULED = "SCHEDULED",
    RESCHEDULED = "RESCHEDULED",
    CANCELLED = "CANCELLED",
    COMPLETED = "COMPLETED"
}

export enum InterviewResult {
    PENDING = "PENDING",
    SELECTED = "SELECTED",
    REJECTED = "REJECTED",
    HOLD = "HOLD"
}